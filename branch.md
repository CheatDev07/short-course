
### The way we can list branches in git repo
```
  git branch -a
```

## the way we can switch branch
```
  git checkout <branchName>
```

## the way we can create branch using command
```
  git branch <newBranch>
```